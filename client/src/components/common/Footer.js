const Footer = () => {
	const link = "nothing";
	const target = "_blank";

	return (
		<div className="container">
			
			<a href={link} target={target}>
				
			</a>
		</div>
	);
};

export default Footer;
